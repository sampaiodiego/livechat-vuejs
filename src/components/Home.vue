<template>
  <div>
				<!--{{#if livechatOnline}}
					{{#if showRegisterForm}}
						{{> register}}
					{{else}}
						{{> messages}}
						{{#if showSwitchDepartmentForm}}
							{{> switchDepartment}}
						{{/if}}
					{{/if}}
				{{else}}-->
					<!--<div class="offline">-->
						<!--{{> offlineForm (offlineData)}}-->
					<!--</div>-->
				<!--{{/if}}-->
  </div>
</template>

<script>
export default {
  name: 'Home'
}
</script>
